# Temporal Fusion Transformer Configuration
# Inflation Prediction - Multi-Horizon Quantile Forecasting

# Data Configuration
data:
  # ============================================================
  # TICKER DATA SOURCE (Polygon.io → BigQuery)
  # ============================================================
  tickers:
    # List of ticker symbols to include (all will use same frequency and features)
    # NOTE: All tickers below are available in BigQuery (see configs/tickers.yaml)
    symbols:
    # ========================================
    # A. EQUITY MARKET INDICES
    # ========================================
    # Primary economic bellwethers - anticipate inflation 3-6 months ahead
    - SPY      # S&P 500 - Primary economic bellwether, inflation-driven earnings signal
    - QQQ      # Nasdaq 100 - Inflation-sensitive via discount rate (growth stocks)
    - IWM      # Russell 2000 - Domestic demand & wage inflation proxy
    - RSP      # Equal-Weight S&P 500 - Market breadth, narrows during inflation uncertainty
    
    # ========================================
    # B. SECTOR ETFs (Sector Rotation)
    # ========================================
    # Different sectors lead in different economic phases
    - XLF      # Financials - Credit creation, early expansion leader
    - XLI      # Industrials - Early cyclical recovery
    - XLY      # Consumer Discretionary - Demand recovery signal
    - XLK      # Technology - Growth phase, rate-sensitive
    - XLE      # Energy - Direct inflation transmission via oil prices
    - XLB      # Materials - Cost-push inflation signal
    - XLP      # Consumer Staples - Inflation resilience, pricing power
    - XLV      # Health Care - Defensive, late-cycle outperformer
    - XLU      # Utilities - Inflation-sensitive through rate costs
    - XLRE     # Real Estate - Inflation hedge but rate-sensitive
    
    # ========================================
    # C. FIXED INCOME (Interest Rate Environment)
    # ========================================
    # Treasury yields embed purest inflation expectations
    - TLT      # 20+ Year Treasury - Long-term inflation expectations
    - IEF      # 7-10 Year Treasury - Nominal growth & inflation outlook
    - TIP      # TIPS - Explicit inflation breakeven measure
    - SHY      # 1-3 Year Treasury - Short-term rates
    
    # ========================================
    # D. CREDIT MARKETS (Credit Cycle)
    # ========================================
    # Credit risk rises when inflation erodes margins
    - LQD      # Investment Grade Corporate Bonds
    - HYG      # High Yield Bonds - Inflation-driven rate hike sensitivity
    
    # ========================================
    # E. COMMODITIES (Inflation Drivers)
    # ========================================
    # Core inflation drivers, rise early in expansions
    - DBC      # Broad Commodities Index - Core inflation driver
    - USO      # Crude Oil - Energy cost inflation
    - GLD      # Gold - Inflation hedge, policy distrust indicator
    - CPER     # Copper - Industrial demand proxy
    
    # ========================================
    # F. AGRICULTURE (Target & Feature)
    # ========================================
    # Food inflation proxies - both predictors and prediction targets
    - DBA      # Agriculture ETF - Broad agricultural commodity basket
    - WEAT     # Wheat ETF - Staple grain, global food supply
    - SOYB     # Soybean ETF - Protein source, livestock feed
    # RJA removed: 55% missing data (625/1393 days) causes NaN after normalization
    
    # ========================================
    # G. CURRENCY (Dollar & Inflation Transmission)
    # ========================================
    # Dollar acts as global inflation valve
    - UUP      # US Dollar Index - Strong USD → imported disinflation
    
    # Shared frequency for all tickers
    frequency: 'daily'
    
    # Shared raw OHLCV features (reduced set - removed open/high/low)
    raw_features:
      - close
      - volume
    
    # Shared technical indicators (synthetic features) for all tickers
    synthetic_features:
      # - returns_5      # 5-period % return  
      # - log_returns    # Log return
      # - volatility_20  # Rolling std of returns (20-period)
      # # - atr_14         # Average True Range (MISSING DATA)
      # - momentum_5     # Price momentum (5-period)
      # - momentum_10    # Price momentum (10-period)
      # # - roc_5          # Rate of Change % (5-period) (EXCLUDED)
      # # - roc_10         # Rate of Change % (10-period) (EXCLUDED)
      # - volume_ma_5    # Volume moving average (5-period)
      # - volume_ma_10   # Volume moving average (10-period)
      # - volume_ratio   # Volume / volume_ma_10 (volume strength)
      # # - obv            # On-Balance Volume (MISSING DATA)
      # - high_low_ratio # Daily price range ratio
      # - bb_upper_20    # Bollinger Band upper
      # - bb_middle_20   # Bollinger Band middle
      # - bb_lower_20    # Bollinger Band lower
      # - bb_width       # Bollinger Band width
      # - sma_10
      # Removed: sma_20 (redundant with bb_middle_20)
      - sma_50
      - sma_200
      # - ema_12         # Exponential Moving Average (12-period) (MISSING DATA)
      # - ema_26         # Exponential Moving Average (26-period) (MISSING DATA)
      # - macd           # MACD line (MISSING DATA)
      # - macd_signal    # MACD signal line (MISSING DATA)
      # - macd_hist      # MACD histogram (MISSING DATA)
  
  # ============================================================
  # GDELT SENTIMENT DATA SOURCE (GDELT GKG → BigQuery)
  # ============================================================
  gdelt:
    enabled: true  # Set to true to include GDELT features
    frequency: '1d'  # Must match what's stored in BigQuery (use '1d' not 'daily')
    
    # Topic groups to load (references topic_group_id from gdelt.yaml)
    # You can specify one or multiple topic groups
    # Examples:
    #   - Single: ['inflation_prices']
    #   - Multiple: ['inflation_prices', 'fed_policy', 'commodities']
    topic_groups:
      - 'inflation_prices'  # Load only inflation-related sentiment
    
    # Sentiment features
    features:
      - weighted_avg_tone       # Primary sentiment (-10 to +10)
      - weighted_avg_polarity   # Sentiment strength/intensity
      - num_articles            # Article volume (media attention)
      - num_sources             # Source diversity
    
    # Feature engineering
    normalize_counts: true      # Normalize article/source counts (log1p transform)
    include_lags: true          # Include lagged sentiment (t-1, t-2, etc.)
    lag_periods: [1, 7, 30]     # 1 day, 1 week, 1 month ago (for daily frequency)
  
  # ============================================================
  # GENERAL DATA CONFIGURATION
  # ============================================================
  # Date range for training data
  start_date: '2020-05-01'  # Reduced range to manage memory usage
  end_date: '2025-11-13'    # Future date for ongoing data collection/test split
  train_ratio: 0.7
  val_ratio: 0.15
  test_ratio: 0.15
  
  # Time series parameters
  lookback_window: 192        # 192 * 15min = 48 hours (matches dataset v3)
  prediction_horizons: [4, 8, 16]  # 1h, 2h, 4h ahead (matches dataset v3)
  
  # Target variable
  target: 'agriculture_basket_returns'  # Average return of WEAT, SOYB, RJA (food inflation proxy)
  
  # Normalization
  normalize: true
  normalization_method: 'standard'  # 'standard' or 'minmax'
  
  # Weekend handling (for daily frequency)
  skip_weekends: true  # Remove Saturday/Sunday from dataset (markets closed)
  
  # Forward filling for missing values
  forward_fill:
    enabled: true  # Fill missing values with last known value
    log_stats: true  # Log detailed statistics about forward filling
    max_fill_limit: 5  # Maximum consecutive periods to forward fill (prevents stale data)

# Model Architecture
model:
  # Core TFT parameters
  hidden_size: 128              # Hidden layer dimension
  lstm_layers: 3               # Number of LSTM layers
  attention_heads: 8           # Multi-head attention
  dropout: 0.2  # Increased from 0.15 to help with overfitting
  
  # Quantile prediction
  quantiles: [0.5]            # Start with median only (reduce to regression task)
  
  # Variable selection network (VSN)
  use_variable_selection: true  # Enable VSN for feature importance learning
  
  # Static covariates (constant for each time series)
  static_features:
    - ticker  # Ticker symbol (SPY, QQQ, IWM, RSP) - helps model learn ticker-specific patterns
  
  # Time-varying known features (future known)
  # Note: Current SimplifiedTFT treats all features the same, but we list them separately for clarity
  time_varying_known:
    # - hour_sin              # Cyclical hour encoding
    # - hour_cos
    # Removed: day_sin, day_cos (use is_weekend instead)
    - month_sin             # Cyclical month encoding
    - month_cos
    - is_weekend            # Binary weekend indicator
  
  # Time-varying unknown features (past-only, not known in future)
  time_varying_unknown:
    # ===== TICKER FEATURES (will be pivoted to ticker-specific) =====
    - close                    # Will become: close_IWM, close_QQQ, close_RSP, close_SPY
    - volume                   # Will become: volume_IWM, volume_QQQ, volume_RSP, volume_SPY
    - sma_50                   # Will become: sma_50_IWM, sma_50_QQQ, sma_50_RSP, sma_50_SPY
    - sma_200                  # Will become: sma_200_IWM, sma_200_QQQ, sma_200_RSP, sma_200_SPY
    
    # ===== GDELT SENTIMENT FEATURES (SHARED ACROSS TICKERS) =====
    - weighted_avg_tone        # News sentiment (-10 to +10)
    - weighted_avg_polarity    # Sentiment strength
    - num_articles             # Media attention (log-normalized)
    - num_sources              # Source diversity (log-normalized)
    # Lagged sentiment (if enabled in data.gdelt.include_lags)
    - sentiment_lag_1          # Sentiment 1 day ago
    - sentiment_lag_7          # Sentiment 1 week ago
    - sentiment_lag_30         # Sentiment 1 month ago

# Training Configuration
training:
  # Optimization
  batch_size: 64
  epochs: 100  # Quick run: reduced from 100
  learning_rate: 0.0001  # Reduced from 0.003 to prevent gradient instability
  optimizer: 'adam'
  weight_decay: 0.01  # L2 regularization to prevent overfitting
  gradient_clip_norm: 1.0  # Clip gradients to prevent explosion
  
  # Directional loss (for trading alignment)
  use_directional_loss: false # Disable initially (focus on main regression)
  wrong_direction_weight: 2.0  # Reduce penalty when enabled later
  
  # Learning rate schedule
  lr_scheduler:
    enabled: true
    type: 'reduce_on_plateau'
    factor: 0.5
    patience: 5
    min_lr: 0.00001
  
  # Early stopping
  early_stopping:
    enabled: true
    patience: 5  # Quick run: reduced from 10
    monitor: 'val_loss'
    mode: 'min'
  
  # Loss function
  loss: 'quantile'  # Quantile loss for probabilistic forecasting
  
  # Gradient clipping
  gradient_clip_val: 1.0
  
  # Checkpointing
  # Note: During training, checkpoints are saved to checkpoints/tft/
  #       At the end, the best checkpoint is copied to models/tft/tft_best.pt
  checkpoint:
    enabled: true
    save_top_k: 3           # Keep top 3 best checkpoints (in checkpoints/tft/)
    monitor: 'val_loss'     # Metric to monitor
    mode: 'min'             # 'min' for loss, 'max' for accuracy
    dirpath: 'checkpoints/tft'  # Intermediate checkpoints directory

# Evaluation Configuration
evaluation:
  # Metrics to track
  metrics:
    - 'mae'          # Mean Absolute Error
    - 'rmse'         # Root Mean Squared Error
    - 'mape'         # Mean Absolute Percentage Error
    - 'quantile_loss'
    - 'coverage'     # Quantile coverage (p10, p90)
  
  # Calibration check
  calibration:
    enabled: true
    bins: 10

# Inference Configuration
inference:
  # Batch prediction
  batch_size: 256
  
  # Output format
  output_format: 'dataframe'  # 'dataframe' or 'dict'
  
  # Save predictions
  save_predictions: true
  output_dir: 'temp/predictions'

# BigQuery Configuration
bigquery:
  project_id: null  # Set via environment variable GCP_PROJECT_ID
  dataset_id: 'raw_dataset'
  
  # Ticker data tables
  ticker:
    raw_table: 'raw_ohlcv'           # Template: raw_ohlcv_{ticker}_{frequency}
    synthetic_table: 'synthetic_indicators'  # Template: synthetic_indicators_{ticker}_{frequency}
  
  # GDELT sentiment table
  gdelt:
    table: 'gdelt_sentiment'         # 15-minute aggregated sentiment

# Logging and Monitoring
logging:
  log_level: 'INFO'
  log_dir: 'logs/tft'
  tensorboard: true
  mlflow: false
  
  # Experiment tracking
  experiment_name: 'inflation_tft_quick_run'
  run_name: null  # Auto-generated if null

# Reproducibility
seed: 42

# Hardware
hardware:
  device: 'auto'  # 'auto', 'cpu', 'cuda', 'mps'
  num_workers: 0  # Data loader workers (set to 0 for Docker to avoid shared memory issues)
  pin_memory: false